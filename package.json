{
  "name": "by-night",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:prod": "npm install && npm run-script build && npm run-script minify:js",
    "build": "npx @11ty/eleventy --formats=liquid,html,md,json,gif,jpg",
    "serve": "npx @11ty/eleventy --formats=liquid,html,md,json,gif,jpg --serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "minify:js": "terser js/event-listeners.js -o _site/js/event-listeners.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-beta.3",
    "@11ty/eleventy-navigation": "^0.3.5",
    "jslint": "^0.12.1"
  },
  "dependencies": {
    "@11ty/eleventy-img": "^2.0.1",
    "terser": "^5.16.1"
  }
}
