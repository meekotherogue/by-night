{% assign items = collections.all | eleventyNavigation: eleventyNavigation.key %}
<div class="picker">
  {%- for item in items -%}
    <div class="picker__item">
      <a class="picker__link" href="{{ item.url }}">{{ item.title }}</a>
    </div>
  {%- endfor -%}
</div>