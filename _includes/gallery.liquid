<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>{{ site.title }}</title>
  </head>
  <body>
    <h1>{{ title }}</h1>
    <div>{{ content }}</div>
    <div>
      {% for img in collections[year] %}
      <div><img src="{{ rootDir }}/{{ img }}" alt="{{ img }}" /></div>
      {% endfor %}
    </div>
  </body>
</html>
